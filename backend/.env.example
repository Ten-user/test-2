# Copy to .env and replace with your Neon / Postgres connection string
# Example (Neon): postgresql://<user>:<pass>@<host>:5432/<db_name>
DATABASE_URL=postgresql://user:password@db.host:5432/dbname
# If your provider requires SSL and you want the helper to set ssl: {rejectUnauthorized:false}, set:
# PGSSLMODE=require
PORT=5000
